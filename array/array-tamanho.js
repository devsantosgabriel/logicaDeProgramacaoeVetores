let array_sem_tamanho = [10]; //não indica tamanho do array, apenas insere o número 10 na 1ª posição. 

console.log(array_sem_tamanho.length); //imprime 1 porque consta o número 10 na 1ª posição, no index 0.

let array_com_tamanho = new Array(10); //indico o tamnanho do array, porém é um array vazio em elementos definidos.

console.log(array_com_tamanho.length);//imprime 10 porque consta o tamanho do array, ainda que não tenha elementos definidos.

